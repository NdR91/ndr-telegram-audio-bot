# Telegram token (required)
TELEGRAM_TOKEN=your_telegram_bot_token

# --- OpenAI provider (default) ---
LLM_PROVIDER=openai
OPENAI_API_KEY=your_openai_api_key
# Optional: override model name (default: gpt-4o-mini)
# LLM_MODEL=gpt-4o-mini

# --- Google Gemini provider (alternative) ---
# LLM_PROVIDER=gemini
# GEMINI_API_KEY=your_gemini_api_key
# LLM_MODEL=gemini-1.5-flash
# Or preview/experimental models:
# LLM_MODEL=gemini-2.0-flash-exp

# --- Prompt configuration (optional) ---
# PROMPT_SYSTEM="Sei un esperto di trascrizione audio. Correggi errori automatici, aggiungi punteggiatura, mantieni il significato originale e restituisci SOLO il testo corretto senza commenti."
# PROMPT_REFINE_TEMPLATE="Questo e un testo... {raw_text} ..."
